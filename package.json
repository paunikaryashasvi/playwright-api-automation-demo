{
  "name": "playwright-api-automation-demo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test-dev": "set TEST_ENV=dev && npx playwright test && npx playwright show-report",
    "test-sit": "set TEST_ENV=sit && npx playwright test && npx playwright show-report"
  },
  "keywords": [
    "playwright",
    "typescript",
    "api-testing",
    "nodejs"
  ],
  "author": "Osanda Nimalarathna",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.13.6",
    "dotenv": "^17.3.1",
    "playwright": "^1.42.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.1",
    "@types/node": "^20.11.30"
  }
}
